# Запрос количества билетов
num_tickets = int(input("Введите количество билетов: "))

# Переменная для хранения общей стоимости
total_cost = 0

# Цикл для ввода возраста и вычисления стоимости для каждого билета
for _ in range(num_tickets):
    age = int(input("Введите возраст посетителя: "))
    
    if age < 18:
        ticket_cost = 0  # Бесплатно для лиц младше 18 лет
    elif 18 <= age < 25:
        ticket_cost = 990  # Стоимость 990 рублей для лиц от 18 до 24 лет
    else:
        ticket_cost = 1390  # Полная стоимость 1390 рублей для лиц старше 24 лет
    
    total_cost += ticket_cost

# Применение скидки, если заказано больше трех билетов
if num_tickets > 3:
    total_cost *= 0.9  # 10% скидка

# Вывод общей стоимости
print(f"Общая стоимость к оплате: {total_cost} руб.")
